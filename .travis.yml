language: cpp

compiler:
  - clang

install: ./install_dependencies.sh

before_script:
  - travis_retry bundle
   -  travis_retry cmake -Dtest=on . && make test

branches:
  only:
    - master
    
notifications:
  email: true
