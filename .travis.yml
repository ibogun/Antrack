language: cpp

compiler:
  - clang

sudo: required

install: ./install_dependencies.sh

before_script:
  - travis_retry bundle
   -  travis_retry cmake -Dtest=on . && make test

branches:
  only:
    - master
    
notifications:
  email: true
